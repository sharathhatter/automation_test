<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <include
        android:id="@+id/toolbarMain"
        layout="@layout/uiv3_tool_layout" />

    <TextView
        android:id="@+id/txtAction"
        style="@style/BottomButtonStyle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:clickable="true"
        android:gravity="center"
        android:orientation="horizontal"
        android:padding="@dimen/padding_normal"
        android:text="@string/saveThisLocation"
        android:textColor="@color/white"
        android:textSize="@dimen/large_text_size" />

    <FrameLayout
        android:id="@+id/layout_map"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/txtAction"
        android:layout_below="@id/toolbarMain" />

    <AutoCompleteTextView
        android:id="@+id/aEditTextChooseArea"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/toolbarMain"
        android:layout_margin="@dimen/margin_normal"
        android:background="@drawable/map_autocompleteview_background"
        android:elevation="10dp"
        android:hint="@string/typeYourLocality"
        android:imeOptions="actionDone"
        android:padding="@dimen/padding_normal"
        android:singleLine="true" />
</RelativeLayout>