<?xml version="1.0" encoding="utf-8"?>

<com.bigbasket.mobileapp.view.uiv3.BBDrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- As the main content view, the view below consumes the entire
         space available using match_parent in both dimensions. -->

    <include layout="@layout/uiv3_main_sub_content_layout" />

    <!-- android:layout_gravity="start" tells DrawerLayout to treat
         this as a sliding drawer on the left side for left-to-right
         languages and on the right side for right-to-left languages.
         The drawer is given a fixed width in dp and extends the full height of
         the container. A solid background is used for contrast
         with the content view. -->

    <LinearLayout
        style="@style/RightNavDrawer"
        android:id="@+id/right_drawer"
        android:layout_height="match_parent"
        android:layout_gravity="right"
        android:background="@color/white"
        android:clickable="true"
        android:orientation="vertical">

        <include layout="@layout/uiv3_spend_trends_filter_layout" />

        <include layout="@layout/uiv3_spend_trends_empty_filter" />
    </LinearLayout>
</com.bigbasket.mobileapp.view.uiv3.BBDrawerLayout>